# Only needed when the client is deployed separately from the API (e.g. client on Vercel,
# API on Railway). Leave unset for local dev — the Vite dev server proxies /api to
# localhost:4000 (see vite.config.ts), and a same-origin production deploy would too.
VITE_API_URL=https://your-api-domain.up.railway.app
